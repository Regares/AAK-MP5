#include <stdio.h>
#include <math.h>

int main()
{
    float a, b, c, res;
    printf("Введите координату точки A через пробел: ");
    scanf("%f", &a);
    printf("Введите координату точки B через пробел: ");
    scanf("%f", &b);
    printf("Введите координату точки С через пробел: ");
    scanf("%f", &c);
    if (((a < c) && (c < b)) || ((b < c) && (c < a)))
    {
        if (a < c)
        {
            res = (c - a) * (b - c);
            printf("Произведение отрезков АС и ВС равно: %f", res);
        }
        else
        {
            res = (c - b) * (a - c);
            printf("Произведение отрезков АС и ВС равно: %f", res);
        }
    }
    else
    {
        printf("Точка С не находится между точками А и В.");
    }
    return 0;
}
